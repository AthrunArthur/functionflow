#SET(SRC_LIST test_interface.cpp)
#INCLUDE_DIRECTORIES(${INCLUDE_DIRS})
#add_definitions(-g)
#LINK_DIRECTORIES(${TOOLS_LINK_DIRS})
#ADD_DEFINITIONS(${DEBUG_DEFS})
#ADD_EXECUTABLE(test ${SRC_LIST})
#TARGET_LINK_LIBRARIES(test ${TOOLS_LINK_LIBS} ff pthread)

SET(FIB_SRC fib.cpp)
INCLUDE_DIRECTORIES(${INCLUDE_DIRS})
add_definitions(-g)
LINK_DIRECTORIES(${TOOLS_LINK_DIRS})
ADD_DEFINITIONS(${DEBUG_DEFS})
ADD_EXECUTABLE(fib ${FIB_SRC})
TARGET_LINK_LIBRARIES(fib ${TOOLS_LINK_LIBS} ff pthread)

SET(QSORT_SRC quick_sort.cpp)
INCLUDE_DIRECTORIES(${INCLUDE_DIRS})
add_definitions(-g)
LINK_DIRECTORIES(${TOOLS_LINK_DIRS})
ADD_DEFINITIONS(${DEBUG_DEFS})
ADD_EXECUTABLE(quick_sort ${QSORT_SRC})
TARGET_LINK_LIBRARIES(quick_sort ${TOOLS_LINK_LIBS} ff pthread)

SET(HELLO_SRC hello_world.cpp)
INCLUDE_DIRECTORIES(${INCLUDE_DIRS})
add_definitions(-g)
LINK_DIRECTORIES(${TOOLS_LINK_DIRS})
ADD_DEFINITIONS(${DEBUG_DEFS})
ADD_EXECUTABLE(hello ${HELLO_SRC})
TARGET_LINK_LIBRARIES(hello ${TOOLS_LINK_LIBS} ff pthread)

SET(TEST_RB_SRC test_ring_buff.cpp)
INCLUDE_DIRECTORIES(${INCLUDE_DIRS})
add_definitions(-g)
LINK_DIRECTORIES(${TOOLS_LINK_DIRS})
ADD_DEFINITIONS(${DEBUF_DEFS})
ADD_EXECUTABLE(test_rb ${TEST_RB_SRC})
TARGET_LINK_LIBRARIES(test_rb ${TOOLS_LINK_LIBS} ff pthread)
